# vim: syntax=config

[default]
fullscreen=yes
save-position-on-quit
vo=gpu
hwdec=vaapi
gpu-context=wayland
ytdl=yes
ytdl-format=h264/bestvideo[ext=mp4]+bestaudio/best
#ytdl-format=h264/best[height=720][ext=mp4]+bestaudio/best
ytdl-raw-options=ignore-errors=

#########
# Cache #
#########

# Configure the cache to be really big (multiple GBs)
# We have a lot of memory, so why not use it for something

cache=yes
cache-default=4000000                   # size in KB
cache-backbuffer=250000                 # size in KB
demuxer-max-bytes=114748364             # ~100 MiB in bytes

